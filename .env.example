# Database Configuration (Postgres)
POSTGRES_USER=your-postgres-user        # Username for Postgres
POSTGRES_PASSWORD=your-postgres-password  # Password for Postgres
POSTGRES_DB=your-database-name          # Name of the Postgres database

# pgAdmin Configuration
PGADMIN_DEFAULT_EMAIL=admin@example.com  # Default email for pgAdmin
PGADMIN_DEFAULT_PASSWORD=your-pgadmin-password  # Default password for pgAdmin

# Go API Configuration (Web service)
DB_HOST=db                              # Host for the Postgres database (usually 'db' when using Docker Compose)
DB_USER=your-database-user              # Database user for Go API to connect to Postgres
DB_PASSWORD=your-database-password      # Password for the database user
DB_NAME=your-database-name              # Name of the Postgres database the Go API will connect to
